const h1Selecionado = document.querySelector('h1')

console.log(h1Selecionado)

// Retorne o url da página atual utilizando o objeto window
const urlPag = window.location.href 

console.log(urlPag)

// Seleciona o primeiro elemento da página que
// possua a classe ativo

const elementoAtv = document.querySelector('.ativo')

console.log(elementoAtv)

// Retorne a linguagem do navegador

const linguagemNav = window.navigator.language
console.log(linguagemNav)

// Retorne a largura da janela 


const tamanhoJan = window.screen.availWidth

console.log(tamanhoJan)